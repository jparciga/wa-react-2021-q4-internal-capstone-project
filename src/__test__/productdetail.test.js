describe('productDetail test suite', () => {
  test('test product detail page fetching and rendering data from the API', () => {})
  test('test product detail contains labels', () => {})
  test('test product detail page contains a quantity selector and add to cart button', () => {})
  test('test validate number of items selected in quantity are added to the cart', () => {})
  test('test add to cart button disable when stock units available is zero', () => {})
})
